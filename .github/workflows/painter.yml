name: Windows application

permissions:
  contents: read

on:
  push:
    branches: [main, develop]
    paths:
      - "client/**"
      - "painter/**"
      - "scripts/**"
      - "about.toml"
      - "LICENSE"
      - "package.json"
      - "bun.lock"
      - ".gitattributes"
      - ".github/workflows/painter.yml"
  pull_request:
    branches: [main, develop]
    paths:
      - "client/**"
      - "painter/**"
      - "scripts/**"
      - "about.toml"
      - "LICENSE"
      - "package.json"
      - "bun.lock"
      - ".gitattributes"
      - ".github/workflows/painter.yml"

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.12

      - name: Install and check frontend dependencies
        run: |
          bun install --frozen-lockfile
          bun run check

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: painter

      - name: Install cargo-about
        run: cargo install cargo-about --version 0.9.1 --locked --features cli

      - name: Generate embedded application assets
        run: bun run prepare:painter

      - name: Format check
        working-directory: painter
        run: cargo fmt --check

      - name: Clippy
        working-directory: painter
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: Test
        working-directory: painter
        run: cargo test --locked

      - name: Build single-file application
        working-directory: painter
        run: cargo build --release --locked

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: stream-painter-windows-x64
          path: painter/target/release/stream-painter.exe
